# http://r-pkgs.had.co.nz/tests.html

context("getFunctionsFromPackage")

test_that("getFunctionsFromPackage returns a dataframe with columns 'fun' and 'package'", {
  # expect_{equal, identical, match, output, message, warning, error, is}
})

test_that("what does it return on nonexistent package/no objects?", {

})
